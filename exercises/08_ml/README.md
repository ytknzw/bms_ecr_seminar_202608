# Ex8 機械学習

## 問題（`python/`）

| ファイル | 内容 |
|----------|------|
| Must `01_regression.ipynb` | `LinearRegression()`（体重）+ MSE / MAPE |
| Must `02_classification.ipynb` | `KNeighborsClassifier()`（種）+ accuracy / report |
| Stretch `03_unsupervised_explain.ipynb` | PCA・KMeans・置換重要度 |
| Stretch `04_lightgbm.ipynb` | LightGBM（回帰 R2 + 分類 accuracy / CV） |

```bash
cd exercises/08_ml/python
uv run jupyter execute 01_regression.ipynb
uv run jupyter execute 02_classification.ipynb
```

正解は `solutions/*.ipynb`。対訳は `r/`。

同内容の `.py` も残してあります（レガシー／代替）。当日の主経路は Notebook です。
